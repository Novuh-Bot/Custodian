MEMBER_BANNED:
  format: '{} (`{}`) was banned by {} (`{}`) for `{}`'

MEMBER_UNBANNED:
  format: '{} was unbanned by {} (`{}`) for `{}`'

MEMBER_SOFTBANNED:
  format: '{} (`{}`) was softbanned by {} (`{}`) for `{}`'

MEMBER_KICKED:
  format: '{} (`{}`) was kicked by {} (`{}`) for `{}`'

MEMBER_MUTED:
  format: '{} (`{}`) was muted by {} (`{}`) for `{}`'

MEMBER_WARNED:
  format: '{} (`{}`) was warned by {} (`{}`) for `{}`'
